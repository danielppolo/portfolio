<%= simple_form_for([ post, comment ], remote: true) do |f| %>
  <%= f.input :author, placeholder: 'your name' %>
  <%= f.input :content, as: :string, placeholder: 'write something here...' %>
  <%= f.submit 'post' %>
<% end %>
